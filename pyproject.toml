[project]
name = "finance-agent"
version = "0.1.0"
description = "Monolithic financial analysis agent built with LangGraph."
readme = "README.md"
requires-python = "==3.13.*"
dependencies = [
    "mcp[cli]",
    "langchain",
    "langgraph",
    "langchain-openai",
    "pandas",
    "yfinance",
    "tavily-python",
    "ddgs",
    "python-dotenv",
]

 [tool.uv]
